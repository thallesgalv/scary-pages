import styled from 'styled-components';

export const WrapperShape = styled.div`
  position: relative;
  height: 100%;

  svg,
  g,
  path {
    height: 100%;
    width: 100%;
  }

  g {
    position: absolute;
  }
`;
